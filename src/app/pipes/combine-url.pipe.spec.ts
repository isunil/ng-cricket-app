import { CombineUrlPipe } from './combine-url.pipe';

describe('CombineUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CombineUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
